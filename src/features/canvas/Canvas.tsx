/**
 * Stub temporal del Canvas.
 * El siguiente commit lo reemplaza con <Stage> de Konva, reglas, hoja A4 y zoom/pan.
 */
export default function Canvas() {
  return (
    <div
      className="h-full w-full flex items-center justify-center text-muted text-11"
      style={{ background: 'var(--canvas)' }}
    >
      Canvas (Konva) — siguiente commit
    </div>
  );
}
